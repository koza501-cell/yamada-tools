// Re-export the canonical AdSlot from components/common.
// The old format-based stub (leaderboard/rectangle/mobile-banner) is replaced.
export { default } from '@/components/common/AdSlot';
export type { } from '@/components/common/AdSlot';
